1, aof的时间戳添加与恢复
    1）添加时间戳
    2）读取带有时间戳的aof并恢复

2，主从同步时如何处理aof文件的同步：
    1）修改现有的同步机制，在同步当前内存的同时，同步当前活动的aof
        **为了减少复杂度和不可控因素，当前的aof同步过程中，暂时禁止aof rewite。也就是说， aof快照，不必严格的按照整点进行切分
    2）新加入历史同步机制，异步的比较主机的历史快照和本机的历史快照，找到缺失部分，并同步
    
